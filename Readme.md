## FED liquidity monitor


#### Create SQLlite DB
1) open python in terminal
2) launch poetry shell python environment
```
poetry run python
```

2) Insert the following commands
```
from database import ( init_db )
init_db()
```
